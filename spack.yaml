spack:
  view: /opt/view
  specs:
    # - hdf5+mpi ^intel-oneapi-mpi
    - hdf5~mpi
    - esmf@8.4~mpi%intel@2021.9 ^netcdf-c~blosc~szip~zstd ^diffutils@3.8

  config:
    install_tree:
      root: /opt/spack
      padded_length: 128

  packages:
    all:
      require: 'target=x86_64_v3'

  mirrors:
    local-buildcache:
      url: oci://ghcr.io/noaa-oar-arl/nexus-spack-buildcache
      signed: false
