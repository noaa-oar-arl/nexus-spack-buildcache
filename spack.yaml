spack:
  view: /opt/view
  specs:
    - esmf@8.4%intel ^netcdf-c~blosc~szip~zstd ^intel-oneapi-mpi ^diffutils@3.8

  config:
    install_tree:
      root: /opt/spack
      padded_length: 128

  packages:
    all:
      require: 'target=x86_64_v3'

  mirrors:
    local-buildcache:
      url: oci://ghcr.io/noaa-oar-arl/nexus-spack-buildcache
      signed: false
